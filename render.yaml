# render.yaml
services:
  - type: web
    name: ai-business-auditor
    env: python
    region: frankfurt
    plan: free
    branch: main
    dockerfile: Dockerfile
    dockerContext: .
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0